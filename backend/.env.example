# Database
DATABASE_URL="postgresql://opsmanager:dev_password_change_in_production@localhost:5432/opsmanager_dev"

# Redis
REDIS_URL="redis://localhost:6379"
REDIS_HOST="localhost"
REDIS_PORT="6379"
REDIS_PASSWORD=""
REDIS_DB="0"

# JWT Configuration
JWT_SECRET="change-this-to-a-secure-random-string-min-32-characters-long"
JWT_ACCESS_EXPIRY="24h"
JWT_REFRESH_EXPIRY="7d"

# Encryption (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))")
SODIUM_MASTER_KEY="your-32-byte-base64-encoded-key-change-in-production"

# SMTP Configuration (optional - uses MailHog in development)
SMTP_HOST="localhost"
SMTP_PORT="1025"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="noreply@opsmanager.local"
SMTP_FROM_NAME="OpsManager"
SMTP_SECURE="false"

# Application
NODE_ENV="development"
PORT="3001"
FRONTEND_URL="http://localhost:3000"

# Rate Limiting
RATE_LIMIT_TTL="60"
RATE_LIMIT_MAX="100"

# Session Configuration
SESSION_TTL="604800"

# Default Admin (for seeding)
DEFAULT_ADMIN_EMAIL="admin@opsmanager.local"
DEFAULT_ADMIN_USERNAME="admin"
